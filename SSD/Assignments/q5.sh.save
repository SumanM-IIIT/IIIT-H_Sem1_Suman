#!/bin/bash
clear
read -p "Enter a value: " str
len=$(echo $str | wc -c)
len=$(expr $len - 1)
#echo $len
x=1
y=$(expr $len / 2)
while test $x -le $y do
	p=$(echo $str | cut -c $i)
	q=$(echo $str | cut -c $len)
	if test $p != $q then
		echo "No"
		exit

	fi
done
				
